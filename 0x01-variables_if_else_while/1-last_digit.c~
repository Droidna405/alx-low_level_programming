#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - prints the last value of the number stored in the variable n
 * 
 * Return: zero 
*/
int main(void)
{
  int n;

  srand(time(0));
  n = rand() - RAND_MAX / 2;
  /* your code goes there */
  return (0);
}
