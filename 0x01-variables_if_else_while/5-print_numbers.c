#include <stdio.h>
/**
 * main - entry point of code
 *
 * Description: Prints integers 0 - 9 followed by a new line
 *
 * Return: Always (0) for success
 */
int main(void)
{
int n;
for (n = 0 ; n <= 9; n++)
{
printf("%d", n);
}
printf("\n");
return (0);
}

